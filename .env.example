# Example environment configuration for Folder File Processor
# Copy this file to .env and update the paths for your environment

# Required - Basic file processing
SOURCE_FOLDER=/path/to/source/folder
SAVED_FOLDER=/path/to/saved/folder
ERROR_FOLDER=/path/to/error/folder

# Optional - Advanced Document Processing with RAG
ENABLE_DOCUMENT_PROCESSING=true
DOCUMENT_PROCESSOR_TYPE=rag_store
MODEL_VENDOR=google  # or openai

# Required if using document processing
GOOGLE_API_KEY=your_google_api_key_here  # if MODEL_VENDOR=google
OPENAI_API_KEY=your_openai_api_key_here  # if MODEL_VENDOR=openai

# ChromaDB Configuration (choose embedded or client_server mode)
CHROMA_CLIENT_MODE=client_server #embedded or client_server
# For embedded mode:
CHROMA_DB_PATH=./data/chroma_db_google
# For client_server mode:
CHROMA_SERVER_HOST=localhost
CHROMA_SERVER_PORT=8000
# Optional - Custom collection name (defaults to documents_[vendor])
CHROMA_COLLECTION_NAME=rag-kb