# Environment template for GHCR deployment
# This file shows the environment variables that will be generated by the deployment script

# Docker image configuration
DOCKER_IMAGE=ghcr.io/ronsonw/kiro-project:latest

# Local folder mappings (set by deployment script)
SOURCE_FOLDER=/path/to/source
SAVED_FOLDER=/path/to/saved
ERROR_FOLDER=/path/to/error

# Document processing configuration
ENABLE_DOCUMENT_PROCESSING=true
DOCUMENT_PROCESSOR_TYPE=rag_store
MODEL_VENDOR=google

# ChromaDB configuration
CHROMA_CLIENT_MODE=embedded
CHROMA_DB_PATH=./data/chroma_db
CHROMA_COLLECTION_NAME=rag-kb

# File monitoring configuration (Docker-optimized)
FILE_MONITORING_MODE=auto
POLLING_INTERVAL=2.0
DOCKER_VOLUME_MODE=true

# Logging configuration
LOG_LEVEL=INFO

# API Keys (loaded from .env.local)
# OPENAI_API_KEY=your_openai_key_here
# GOOGLE_API_KEY=your_google_key_here